Порождающие паттерны (основные):

- фабричный метод (fabric method)
- фабрика классов (class fabric)
- singletone
- builder


Сериализация объектов 
class1, class2, ... -> str (xml, json, + bson, yaml, ...)
Строим решение от плохого к хорошему
